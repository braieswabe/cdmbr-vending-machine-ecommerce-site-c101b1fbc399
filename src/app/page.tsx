export default function Page() {
  return (
    <main className="min-h-screen grid place-items-center p-8">
      <section className="max-w-2xl">
        <h1 className="text-3xl font-bold mb-2">Dudex Template Ready</h1>
        <p className="text-gray-600">
          This repository is configured as the base template for deterministic
          OpenClaw project generation.
        </p>
      </section>
    </main>
  );
}
